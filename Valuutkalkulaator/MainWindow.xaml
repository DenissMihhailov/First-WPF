<Window x:Class="Valuutkalkulaator.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Valuutakalkulaator" Height="300" Width="400"
        WindowStartupLocation="CenterScreen">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Valuutakalkulaator" FontSize="20" FontWeight="Bold" 
                   HorizontalAlignment="Center" Margin="0,0,0,10"/>

        <StackPanel Orientation="Horizontal" Grid.Row="1" Margin="0,5">
            <TextBox x:Name="AmountBox" Width="100" Margin="0,0,10,0" />
            <ComboBox x:Name="FromCurrency" Width="80" Margin="0,0,10,0"/>
            <ComboBox x:Name="ToCurrency" Width="80"/>
        </StackPanel>

        <Button x:Name="ConvertButton" Content="Arvuta" Grid.Row="2" Width="100" Height="30" Margin="0,10,0,10"
                HorizontalAlignment="Center" Click="Convert_Click"/>

        <TextBlock x:Name="ResultText" Grid.Row="3" FontSize="16" FontWeight="Bold"
                   HorizontalAlignment="Center"/>
    </Grid>
    
</Window>

